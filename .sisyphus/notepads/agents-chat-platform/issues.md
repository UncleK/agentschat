# Issues

- 2026-03-28: `flutter test integration_test/app_shell_navigation_test.dart -d windows` was blocked by `No Windows desktop project configured.` Resolved by generating the default `app/windows/**` runner in-place.
- 2026-03-29: Runtime verification on this machine could not bind to port `3000` because Windows reports an excluded TCP range covering `2924-3023`; the Task 3 Nest boot fix was therefore confirmed on port `3100` after app initialization proved the migration-loading crash was resolved.
- 2026-03-29: Task 5 live QA initially failed because `pnpm start` was launched without the required backend env vars and then, after restart, because the shared runtime database had not yet applied `1710000002000-AddFederationTransportPersistence.ts`. Resolved by booting on port `3100` with explicit test env values and running `pnpm migration:run` before the live HTTP checks.
- 2026-03-29: Task 6 live QA initially exposed `assets.created_by_user_id` getting nulled during completion because `assetRepository.save(asset)` re-persisted a nullable relation field; resolved by changing completion to `update(...)` + refetch before serializing the asset.
- 2026-03-30: The continuation summary referenced `.sisyphus/boulder.json`, but that file was not present during Task 10 work. Progress tracking used the plan file plus real filesystem/test state instead.
- 2026-03-30: Task 11 follow-up verification initially failed because older content/moderation tests seeded live debates without pre-created pending turns, and `ensurePendingTurn()` also tripped a TypeScript deep-partial error when writing nullable `assignedAgentId`. Resolved by localizing the compatibility fix to `debate.service.ts` and keeping the spectator regression aligned with the approved product rule.
- 2026-03-30: Hands-on API QA after the Task 11 regression pass found `GET /api/v1/debates/not-a-real-debate` and `/archive` returning 500 because malformed UUIDs were not rejected until deeper persistence code. Resolved by adding controller-level UUID parsing and a targeted debate endpoint test for malformed-vs-missing IDs.
- 2026-03-31: Task 12 full Flutter verification initially failed because the old `app_shell_test.dart` still asserted the removed Hub placeholder transport strings; resolving it required updating the shell test to assert the new Hub QA keys instead of deleted placeholder text.

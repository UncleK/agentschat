# Codex 执行提示词 · v1.1

本提示词与修复计划内版本一致。应随整个 v1.1 交付包一起交给 Codex；不包含自动修改生产环境的授权。

```text
请修复当前 agentschat 工作区中的审计问题。使用 AgentsChat Audit v1.1，而不是仅依据旧版修复提示词。

先阅读 PRODUCT_CONTRACT.md、AUDIT_REPORT.md、findings.json 与 REMEDIATION_PLAN.md。基准提交为 1fe8354186009259c11df3a4cbb0adbd80325818；执行前读取当前 HEAD 和差异，逐条复核涉及代码。报告不是要求你盲目信任每项结论，误报或已修复项需给出源码与测试证据。

已确认、不可删除的产品目标：人类尚未注册 APP 时，可以先让自己的 Agent 通过 public 路线建立身份并按平台策略参与；人类以后注册／登录，再经原控制端授权绑定同一个 Agent。先注册再 bound 接入的路线也须保留。
Self-owned 在旧代码中应理解为“未绑定站内人类账户”，不是无主、公共可领取的 Agent，也不是自主程度。前端可澄清名称，但不要仅为改名无计划地破坏数据库枚举、协议或旧运行端。是否绑定账户与 isPublic、内容策略、自主活动程度分别处理。
禁止通过强制预注册、给全部公开入口统一加 HumanAuthGuard、取消 public 路线、将所有未绑定 Agent 改为只读或删除旧身份来修复。先阅读并保留现有合法能力；数据和 agentId 必须延续。

优先完成批次 A、B，再依次 C 至 F。每条确认的问题先补旧实现失败、新实现通过的回归测试，再做最小可行修复。产品保留用例可能旧版已经通过，修复后必须继续通过，不能为了“所有新用例先失败”人为破坏它们。
CL-01～CL-04 与 ON-01～ON-10 都是必要验收；只阻断恶意请求却使免注册接入或后续正常绑定失效，不算修复完成。

AC-01：关闭或重构缺少原控制端授权的人类单方绑定逻辑，不是删除所有人类确认或 claim 功能。站内账户的有效登录、目标 Agent 原控制端的有效凭证及操作者对该账户关联的明确授权必须同时成立。请求的账户、用途、有效期与目标不得混淆；事务中一次性消费请求并更新关联及审计。未指定目标的请求只能在授权确认时落定一次。
公共私信、论坛文本、claim.requested 推送、显示名和模型生成的“我同意”不能触发控制授权。使用可信本地或远程管理入口及确定性处理；社交执行上下文不得越权调用该入口。不要只要求模型识别“主人”。
申请者拿到 challenge 本身可以是交接步骤，但绝不能仅凭该值自行完成绑定。非法待处理申请不能占住目标，原控制者应仍可按安全流程完成关联。绑定前保护历史私信，绑定后明确管理范围、保留身份与所有既有记录，不自动公开内容。

AC-02：公开资料不得写 owner／platform／storage 或主人策略保留字段；未绑定端仍受平台安全默认值和原运行环境的更严格本地权限控制，不要求补人类注册才能正常参与。
AC-03／04：头像覆盖真实字节校验与公开读取隔离；Webhook 覆盖解析、实际连接和重定向限制。短期可以在有明确兼容和恢复方案下关闭危险 Webhook、保留 polling；不得把这变成取消 public 接入。
AC-05：保留账户限流与容量保护，修复可信代理来源，不关闭所有限流或盲目信任转发头。
AC-06／07：Accepted action、业务事件、outbox 和投递须有可恢复持久状态、租约、幂等和并发约束，不依赖进程内 Map 保证可靠性。
AC-11：public 与 bound 两种接入都隔离公共社交执行和原操作者管理能力。使用执行层工具控制和受保护的授权路径，不能只增加提示词。
其余项目按对应发现与验收矩阵实施，保留 Web／Flutter 登录撤销、正常插件交互及 Human 不冒充 Agent 作者的规则。

禁止只隐藏前端入口、删除测试、放宽断言或靠 skip 过关。不得无关升级整个依赖树、顺带重写产品或将所有能力改成新架构才可用。使用项目锁文件对应版本与官方资料核验 API。
测试只用隔离环境和合成数据，不扫描生产内网、不读取真实私信、不使用真实凭证、不自动实施破坏性生产迁移。仓库保护与线上配置需要相应授权；先在独立修复分支工作。

每批输出：已复核发现、改动文件、兼容和迁移影响、实际命令和测试结果、未覆盖边界、回滚方式。记录免注册接入、后续同身份绑定和 bound 路线是否仍通过。原 WebSocket probe 是旧缺陷基线，不是修复验收；其旧结果文件不是本轮运行结果。
工具或环境受阻时明确标记未运行，不把计划写成验证通过。完成后按剩余安全风险、产品正向验收与 required checks 分别判断是否满足扩大开放条件。
```

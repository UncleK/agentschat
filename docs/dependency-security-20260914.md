# 依赖安全修复与插件兼容验证（2026-09-14）

## 告警来源

读取 GitHub 当前 164 条开放 Dependabot 告警：142 条来自 OpenClaw 插件锁文件，22 条来自后端锁文件。按严重性分为 1 critical、79 high、69 medium、15 low；均被 GitHub 标为 development 依赖，其中部分会随安装或构建工具存在于运行环境，不能据此忽略。

原插件把 OpenClaw 固定在 2026.4.23，并用旧覆盖规则保留了多层过期传递依赖。后端主要为工具链的间接依赖。

## 已修改

- 插件版本准备为 `0.2.0`；开发依赖使用 `^2026.9.4`，宿主范围为 `>=2026.9.4 <2027.0.0`。锁文件记录本次验证的 OpenClaw 2026.9.4。Node 要求跟随上游：24.16+ 的 24.x 或 26.1+。
- 移除旧插件覆盖规则，由更新后的上游依赖树提供修复。适配新 SDK 的不可变配置快照、聚焦配置变更和 `runEmbeddedAgent`；会话保持原有稳定身份，由宿主选择存储格式。配置写入在宿主的最新草稿中修改，保留其他 Agent 槽位和并发设置。
- 后端在现有主要版本内升级框架及开发依赖；对 `glob@>=10.2.0 <10.5.0` 使用同一主要版本的安全修复范围。`agentscant` 从短 Git 提交标识改为同一个提交的完整标识，未改变其源码版本。
- 新版 ESLint/Prettier 所要求的格式与测试类型修正通过检查，未改变后端业务规则。
- 添加每周 Dependabot 更新、安全审计 CI、插件锁定依赖/最新兼容依赖两组验证、README 生成检查、CodeQL 工作流和私密漏洞报告说明 `SECURITY.md`。

## 验证与边界

- 三个工作区的全依赖审计均为 0 已知漏洞（包括开发依赖）。逐条比较 164 条 GitHub 告警的包名与版本范围，本地新锁文件命中 0 条。
- 插件构建与 6 项测试通过：真实 SDK 加载完整/设置入口，模拟运行时验证配置隔离、最新草稿写入、稳定会话、模型选择、回复限制、NO_REPLY、空回复和公开资料生成。打包检查通过，包含新运行时辅助文件。
- 后端 lint、typecheck、build、37 项单元测试、32 项集成测试和 89 项端到端测试通过。集成与端到端测试使用本地隔离数据库、Redis 测试库和独立 MinIO 测试桶。系统 Node 24.15.0 首次端到端进程异常退出；在隔离的 Node 24.16.0 下完整重跑通过。
- Web 45 项测试、生产构建、18 个中英文公开页面发现检查，以及文档页面 24 组桌面/手机布局检查通过。
- 本机系统 Node 为 24.15.0，插件验证使用隔离的 Node 24.16.0，没有修改全局 Node 或实际 Agent 运行环境。
- 未进行真实模型调用、Agent 接入或外部发信。此批次按 `main` 与 `stable` 同步后发布服务器；实际部署提交以服务器 `.source-commit` 为准。npm/ClawHub 包发布是独立步骤，此批次没有执行。GitHub 上的告警计数和 CodeQL 状态需在默认分支更新并执行扫描后核实。Secret scanning 的仓库开关本次未更改。

本地审计快照保存在忽略提交的 `output/security/` 下：原始告警、三份 audit JSON、逐条比较结果及打包清单。零依赖告警不能替代源码审计或真实运行验证。

## GitHub 上线复核

2026-09-14 推送后，164 条 Dependabot 告警由依赖更新自动关闭，开放数量为 0。README、Web（含发布失败恢复）、后端、Flutter、插件锁定依赖与最新兼容依赖的 CI 全部通过。

新启用的 CodeQL 完成扫描后出现 11 项提示。按来源和实际使用位置逐项核对后标记为误报，每项都保留了 GitHub 审查备注；没有关闭扫描或批量排除源码：

| 告警 | 核对依据 |
| --- | --- |
| #1，会话标识的 SHA-1 | 输入只有公开 Agent 名、场景与 threadId，用于保持会话标识，不参与认证或凭证保护；改变它会丢失原会话关联。 |
| #2，密码哈希 | 命中的是带服务端密钥的验证码 HMAC，具有用途、时效、次数和消费检查；实际密码使用随机盐 scrypt。 |
| #3 / #11，登录跳转 | `safeReturnPath` 限定站内路由，并拒绝反斜杠、控制字符、外部和协议相对 URL；语言转换后仍为站内路径。 |
| #4—#8，检查脚本的 HTML 处理 | 只在 Node 验收脚本中比较页面文字和 markup，没有输出到浏览器、执行或对外响应的路径；不是生产 HTML 清理器。 |
| #9 / #10，附件预览 | `filePreview` 只来自 `URL.createObjectURL(file)`，卸载时释放；React 将生成的 blob URL 用作图片/音频地址，不将 DOM 文本重新解析为 HTML。 |

CodeQL 当前开放数量为 0，误报记录仍可审计。这不等于证明整个项目不存在漏洞。

首次后端 Dependabot 自动更新任务因候选依赖 `ansis@4.4.0` 尚未达到 `minimumReleaseAge` 等待期而暂缓；现有锁文件构建、审计与 CI 均正常。保留等待期，不为消除任务提示强行升级新发布依赖。npm/ClawHub 仍未发布新包。
